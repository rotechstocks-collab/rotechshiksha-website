You are working on rotechshiksha.com (React + TS + Tailwind + shadcn + Radix UI + wouter).
STRICT RULES:
- DO NOT delete any pages/routes.
- DO NOT remove or change the top market ticker (must remain).
- Keep SEBI-safe copy rules intact.
- Make minimal, surgical diffs.

PROBLEM:
At 100% zoom the Home page hero starts too low / extra gap appears below the fixed header+ticker.
Pricing page looks fine, but Home alignment is inconsistent due to dynamic ticker/navbar height and pixel rounding.
Static padding values and CSS var constants are not reliable across zoom levels.

GOAL:
Make layout PERFECT and stable at ALL zoom levels by measuring real header+ticker height and applying it as a CSS variable offset.

------------------------------------------------------------
TASK 1 — Add dynamic header height measurement (mandatory)
In client/src/App.tsx (or the main layout component that renders the ticker + navbar + main):
1) Wrap the header area (ticker + navbar together) in a container with id="app-header".
Example:
<div id="app-header" className="fixed top-0 left-0 right-0 z-50"> ...ticker + navbar... </div>

IMPORTANT:
- DO NOT remove ticker or navbar
- Just ensure the combined header wrapper exists and contains both

2) Add a function to measure height of #app-header and set a CSS variable on :root:
document.documentElement.style.setProperty("--app-header-offset", `${height}px`);

3) Run this measurement:
- on initial mount
- on route change
- on window resize
- after fonts/images load (use requestAnimationFrame + setTimeout 50ms)
Use a safe debounced approach to avoid performance issues.

Use this exact logic:

useEffect(() => {
  const measure = () => {
    const header = document.getElementById("app-header");
    if (!header) return;
    const h = header.getBoundingClientRect().height;
    document.documentElement.style.setProperty("--app-header-offset", `${Math.ceil(h)}px`);
  };

  // run multiple times to account for async rendering/fonts
  const raf1 = requestAnimationFrame(measure);
  const t1 = window.setTimeout(measure, 50);
  const t2 = window.setTimeout(measure, 200);

  const onResize = () => measure();
  window.addEventListener("resize", onResize);

  return () => {
    cancelAnimationFrame(raf1);
    window.clearTimeout(t1);
    window.clearTimeout(t2);
    window.removeEventListener("resize", onResize);
  };
}, [location]);

------------------------------------------------------------
TASK 2 — Use the CSS variable for main padding (mandatory)
Find the <main> element that currently uses:
pt-14 md:pt-[102px] or pt-[var(--app-header-offset)] with constant values etc.
Replace it with ONLY:

<main className="pt-[var(--app-header-offset)] relative min-h-[100svh] ...">

If the class includes z-0, change to z-10.

------------------------------------------------------------
TASK 3 — Add a default fallback in CSS (mandatory)
In client/src/index.css add:

:root { --app-header-offset: 104px; }

This ensures safe padding before JS measurement runs.

------------------------------------------------------------
VALIDATION:
After changes:
- At 100% zoom, Home hero should start perfectly under ticker+navbar (no extra gap).
- At 90%, 110%, 125% zoom, still perfect.
- Navigate Home -> Pricing -> Home: always aligned.
- Ticker unchanged and visible.
- No routes deleted.
Deliver a diff summary and ensure TypeScript builds.