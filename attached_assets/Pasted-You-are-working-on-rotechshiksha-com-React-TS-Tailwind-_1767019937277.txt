You are working on rotechshiksha.com (React + TS + Tailwind + shadcn + Radix UI + wouter).
IMPORTANT RULES:
- DO NOT delete any pages/routes.
- DO NOT remove or change the top market ticker component.
- Keep SEBI-safe copy rules intact.
- Make minimal, surgical diffs only.

GOAL:
Permanently fix the recurring “100% zoom / content clip-disappear / scroll stuck” issue caused by Radix scroll-lock not cleaning up after route changes, theme toggles, or stale portals.

THIS IS NOT OPTIONAL:
You MUST fully remove the overlay-check logic from App.tsx.
The current code uses hasOpenOverlay() and safeUnlockScroll(). These are causing the bug to persist.
They MUST be deleted and replaced with unconditional hard unlock logic.

----------------------------------------------------------------
TASK 1 — Fix App.tsx (mandatory)
File: client/src/App.tsx

A) Find and DELETE the following functions entirely:
- hasOpenOverlay()
- safeUnlockScroll()

B) Replace the existing unlockScroll() with a HARD unlockScroll that resets:
- body overflow, paddingRight, position, top, width
- html overflow, paddingRight
- remove data-scroll-locked from body and html

Use this exact code:

const unlockScroll = useCallback(() => {
  const b = document.body;
  const h = document.documentElement;

  b.style.overflow = "";
  b.style.paddingRight = "";
  b.style.position = "";
  b.style.top = "";
  b.style.width = "";

  h.style.overflow = "";
  h.style.paddingRight = "";

  b.removeAttribute("data-scroll-locked");
  h.removeAttribute("data-scroll-locked");
}, []);

C) Route change must ALWAYS unlock (no overlay checks):
Use this exact effect:

useEffect(() => {
  const t = window.setTimeout(() => unlockScroll(), 0);
  return () => window.clearTimeout(t);
}, [location, unlockScroll]);

D) MutationObserver must FORCE unlock when lock detected:
Inside the observer callback, detect scroll lock via:
- body overflow hidden OR body/html has data-scroll-locked
and then call requestAnimationFrame(unlockScroll) unconditionally.

Use this exact observer logic:

const observer = new MutationObserver(() => {
  const bodyOverflow = window.getComputedStyle(document.body).overflow;
  const hasScrollLock =
    bodyOverflow === "hidden" ||
    document.body.hasAttribute("data-scroll-locked") ||
    document.documentElement.hasAttribute("data-scroll-locked");

  if (hasScrollLock) requestAnimationFrame(unlockScroll);
});

Observe both body and html for:
attributeFilter: ["style", "data-scroll-locked"]

E) Add event listeners to unlock:
- Escape key
- visibilitychange (when tab becomes visible)
- window focus
- window resize

Use exactly:

const onVisibility = () => {
  if (document.visibilityState === "visible") unlockScroll();
};

const onKeyDown = (e: KeyboardEvent) => {
  if (e.key === "Escape") unlockScroll();
};

window.addEventListener("focus", unlockScroll);
window.addEventListener("resize", unlockScroll);
document.addEventListener("visibilitychange", onVisibility);
window.addEventListener("keydown", onKeyDown);

And cleanup them properly in return.

----------------------------------------------------------------
TASK 2 — Add CSS safeguard (mandatory)
File: client/src/index.css

Append these lines at the bottom:

html, body { height: 100%; overflow-x: hidden; }
body[data-scroll-locked] { overflow: hidden !important; }

----------------------------------------------------------------
TASK 3 — Optional but recommended stacking fix
In App.tsx layout, find the <main> element with class containing "relative z-0".
Change only z-0 -> z-10 to prevent stacking/clipping issues at 100% zoom.
Do not change other layout styles.

Example:
<main className="... relative z-10 ...">

----------------------------------------------------------------
VALIDATION (must run and confirm):
After applying changes:
1) At 100% zoom, open Radix dropdown/sheet/dialog -> close -> scroll must not get stuck.
2) Navigate Home -> Pricing -> Home -> scroll must work.
3) Toggle dark theme -> scroll must work.
4) No pages/routes removed, ticker unchanged.

Deliver:
- show code diff summary
- ensure TypeScript compiles and no lint errors