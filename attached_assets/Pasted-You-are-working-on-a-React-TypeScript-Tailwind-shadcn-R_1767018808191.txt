You are working on a React + TypeScript + Tailwind + shadcn + Radix UI + wouter project (rotechshiksha.com).
Do NOT delete any pages/routes. Do NOT remove the top market ticker. Maintain SEBI-safe tone (no tips/guarantees).
Fix two critical bugs:
(1) Radix scroll-lock gets stuck causing content to appear clipped/disappear at 100% zoom or after navigation/theme toggles.
(2) Dark theme toggle makes content invisible because a spotlight background pseudo-element overlays the content.

TASK 1 — Dark theme content overlay fix:

In client/src/index.css, locate the spotlight/background rules for .dark-spotlight-bg::before.
Ensure the pseudo-element is ALWAYS behind content by enforcing a stacking context and negative z-index.
Apply:

.dark-spotlight-bg {
  position: relative;
  isolation: isolate;
}
.dark .dark-spotlight-bg::before {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: -1;
  background:
    radial-gradient(circle at 50% 20%, rgba(56, 189, 248, 0.18), transparent 55%),
    radial-gradient(circle at 80% 30%, rgba(168, 85, 247, 0.14), transparent 60%),
    radial-gradient(circle at 20% 80%, rgba(34, 197, 94, 0.10), transparent 55%),
    linear-gradient(to bottom, rgba(2, 6, 23, 1), rgba(2, 6, 23, 0.95), rgba(2, 6, 23, 1));
}


Make sure no other spotlight pseudo-element has z-index >= 0 that can sit on top of content.

TASK 2 — Global scroll unlock (Radix stuck lock fix):

In client/src/App.tsx, implement a HARD unlockScroll function that resets:

body overflow, paddingRight, position, top, width

html overflow, paddingRight

removes data-scroll-locked on body/html
Use this unlock on every route change using wouter useLocation, with setTimeout(...,0) to run after render.
Replace any “safeUnlockScroll” logic that blocks unlock due to overlay detection. Route change should ALWAYS unlock.

Ensure unlockScroll runs on:

route change

Escape key press

visibilitychange (when returning to tab)

Example hard unlock function:

const unlockScroll = () => {
  const b = document.body;
  const h = document.documentElement;
  b.style.overflow = "";
  b.style.paddingRight = "";
  b.style.position = "";
  b.style.top = "";
  b.style.width = "";
  h.style.overflow = "";
  h.style.paddingRight = "";
  b.removeAttribute("data-scroll-locked");
  h.removeAttribute("data-scroll-locked");
};


Make route change effect unconditional:

useEffect(() => {
  const t = window.setTimeout(() => unlockScroll(), 0);
  return () => window.clearTimeout(t);
}, [location]);


Add basic CSS safeguard in client/src/index.css:

html, body { height: 100%; overflow-x: hidden; }
body[data-scroll-locked] { overflow: hidden !important; }

Validation Checklist:

After changes:

Toggle dark theme: all sections remain visible (no overlay dimming).

Open Radix dropdown/sheet/dialog then navigate: scroll should never remain locked.

At 100% zoom: no clipping or stuck scroll.

No pages/routes removed, ticker stays as-is.

Apply changes as minimal diffs, keep existing UI.