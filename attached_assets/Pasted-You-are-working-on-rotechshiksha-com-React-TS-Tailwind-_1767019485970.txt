You are working on rotechshiksha.com (React + TS + Tailwind + shadcn + Radix UI + wouter). DO NOT delete any pages/routes. DO NOT remove the top market ticker. Keep SEBI-safe copy rules intact. Goal: Permanently fix the “100% zoom / content clip-disappear / scroll stuck” issue caused by Radix scroll-lock not cleaning up after route change, theme toggle, or stale portals. Implement these exact changes: 1) In client/src/App.tsx: - Replace current unlockScroll() with a HARD unlock that resets common scroll lock styles: body overflow, paddingRight, position, top, width html overflow, paddingRight remove data-scroll-locked from body and html - Remove dependency on hasOpenOverlay()/safeUnlockScroll() logic for route changes. On every route change, ALWAYS hard-unlock (unconditional). - In the MutationObserver callback: if scroll lock is detected (body overflow hidden OR data-scroll-locked exists), then call unlockScroll() directly (do not block via overlay checks). - Add event listeners to hard-unlock on: Escape key visibilitychange (when tab becomes visible) window focus window resize Suggested replacement code logic: const unlockScroll = useCallback(() => { const b = document.body; const h = document.documentElement; b.style.overflow = ""; b.style.paddingRight = ""; b.style.position = ""; b.style.top = ""; b.style.width = ""; h.style.overflow = ""; h.style.paddingRight = ""; b.removeAttribute("data-scroll-locked"); h.removeAttribute("data-scroll-locked"); }, []); useEffect(() => { // route change must ALWAYS unlock (after render) const t = window.setTimeout(() => unlockScroll(), 0); return () => window.clearTimeout(t); }, [location, unlockScroll]); useEffect(() => { const observer = new MutationObserver(() => { const bodyOverflow = window.getComputedStyle(document.body).overflow; const hasScrollLock = bodyOverflow === "hidden" || document.body.hasAttribute("data-scroll-locked") || document.documentElement.hasAttribute("data-scroll-locked"); if (hasScrollLock) requestAnimationFrame(unlockScroll); }); observer.observe(document.body, { attributes: true, attributeFilter: ["style", "data-scroll-locked"] }); observer.observe(document.documentElement, { attributes: true, attributeFilter: ["style", "data-scroll-locked"] }); const onVisibility = () => { if (document.visibilityState === "visible") unlockScroll(); }; const onKeyDown = (e) => { if (e.key === "Escape") unlockScroll(); }; window.addEventListener("focus", unlockScroll); window.addEventListener("resize", unlockScroll); document.addEventListener("visibilitychange", onVisibility); window.addEventListener("keydown", onKeyDown); return () => { observer.disconnect(); window.removeEventListener("focus", unlockScroll); window.removeEventListener("resize", unlockScroll); document.removeEventListener("visibilitychange", onVisibility); window.removeEventListener("keydown", onKeyDown); }; }, [unlockScroll]); 2) Add CSS safeguard in client/src/index.css (append): html, body { height: 100%; overflow-x: hidden; } body[data-scroll-locked] { overflow: hidden !important; } 3) IMPORTANT: Do not remove any components (MarketTicker must remain). Keep all routes intact. After changes, validate: - At 100% zoom, scroll never gets stuck after opening/closing dropdowns/sheets/dialogs. - Route navigation always restores scrolling even if a Radix portal is left behind. - Dark theme continues to show content correctly.