✅ PROMPT: Add Premium Dark Theme Gradients + Soft Spotlight Background (No layout change)

You are an expert React + TypeScript + Tailwind UI engineer.

Goal:

Dark theme me site ko premium cinematic look dena hai:
✅ Soft spotlight glow behind hero
✅ Smooth gradient background across pages
✅ Subtle noise / blur effects
✅ Light theme unchanged
✅ No content/layout changes (only styling)

✅ STEP 1: Add global dark gradient background utilities

In client/src/index.css add:

.dark-spotlight-bg {
  @apply relative overflow-hidden;
}

.dark-spotlight-bg::before {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;

  background:
    radial-gradient(circle at 50% 20%, rgba(56, 189, 248, 0.18), transparent 55%),
    radial-gradient(circle at 80% 30%, rgba(168, 85, 247, 0.14), transparent 60%),
    radial-gradient(circle at 20% 80%, rgba(34, 197, 94, 0.10), transparent 55%),
    linear-gradient(to bottom, rgba(2, 6, 23, 1), rgba(2, 6, 23, 0.95), rgba(2, 6, 23, 1));
}


✅ This creates premium spotlight glow only in dark theme.

✅ STEP 2: Apply background class to page wrappers ONLY in dark mode

Wherever main wrapper is like:

<div className="min-h-screen page-bg">


Update to:

<div className="min-h-screen page-bg dark:dark-spotlight-bg">


✅ Ensure wrapper has relative so spotlight sits behind content.

✅ STEP 3: Ensure all page content stays above spotlight

On the main content container inside pages (usually section wrappers), add:

relative z-10


Example:

<section className="py-14 md:py-20 relative z-10">


✅ So text and cards appear above gradients.

✅ STEP 4: Add subtle gradient for hero CTA card (dark mode only)

For hero CTA wrapper cards (like Home hero box), add:

dark:bg-gradient-to-br dark:from-slate-900/70 dark:to-slate-950/60
dark:border-slate-700/50


✅ Helps hero look premium.

✅ VALIDATION CHECKLIST

✅ Dark theme shows spotlight + gradient background
✅ Cards remain glass (from previous prompt)
✅ Light theme unchanged
✅ No layout breaking
✅ Mobile smooth
✅ No route/page deleted
✅ Market ticker stays

Implement cleanly and show code diff.